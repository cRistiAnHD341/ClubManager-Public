## v1.0.0.23 (2025-09-21)

Novedades destacadas
- Registro de Accesos: impresión de listados por partido
  - Nuevos listados de Accesos de Abonados y Accesos por Entrada.
  - Cabecera con título contextual (vs. equipo visitante), totales y filtros aplicados.
  - Paginación automática, anchos de columna ajustados y alineación coherente.
- Ventana “Novedades” tras actualizar
  - Se muestra solo cuando cambia la versión del programa.
  - Lee las notas desde ReleaseNotes.md (carpeta de la app) y muestra la sección de la versión actual si existe.

Mejoras
- Integración de impresión desde Registro de Accesos: botón Imprimir que permite elegir qué listado enviar (Abonados y/o Entradas).
- Mensajería y confirmaciones más claras antes de imprimir (impresora, tamaño de papel, orientación).
- Cabeceras y pies de página en listados más consistentes y con mejor legibilidad.

Correcciones
- Corrección de errores de compilación en ListPrintService (uso de Math.Max).
- Manejo más robusto de configuración de impresoras: mensajes guiados cuando no hay impresora de listados configurada.
- Manejo de errores al construir documentos de impresión y al mostrar resúmenes.

Cómo usar (rápido)
1) Imprimir accesos del partido
   - Navega a Partidos ? abre “Registro de Accesos”.
   - Usa filtros si lo necesitas (texto y “Solo últimos 100”).
   - Pulsa “Imprimir” y selecciona qué listado enviar (Abonados / Entradas).
2) Ver novedades tras actualizar
   - Al iniciar después de actualizar, se abrirá automáticamente la ventana “Novedades”.

Notas técnicas
- Archivo ReleaseNotes.md
  - Incluido como contenido del proyecto y copiado a la carpeta de salida (Build Action: Content, Copy to Output Directory: PreserveNewest).
  - El lector de notas intenta extraer la sección “## vX.Y.Z”. Si no la encuentra, muestra el archivo completo (recortado).
- Actualización obligatoria
  - ApplicationVersion y MinimumRequiredVersion sincronizadas en 1.0.0.23 (ClickOnce).
- Requisitos
  - .NET 9 y Windows (WPF).
